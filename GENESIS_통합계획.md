# GENESIS 메뉴 통합 계획 (내비 재배치)

> 핵심 원칙: **데이터 구조는 안 건드린다. 화면 코드도 거의 그대로.**
> 메뉴(8개)를 3개 공간으로 묶고, 각 공간 안에서 **탭 전환**만 한다. (= 2번 a안)
> "대격변"처럼 보이지만 실제로는 AppShell `navItems` 재배치 수준 → 위험 낮음.

---

## 최종 구조

```
🌍 세계관 (빌드)                🎲 플레이 (런)            🎲 소재뽑기
  [문서] [연표] [관계도] [지도]     [저널] [오라클] [전투]     (독립 유지, 안 건드림)
   └ 탭 전환                        └ 탭 전환
```

- 전역 메뉴: 8개 → **3개** (세계관 / 플레이 / 소재뽑기)
- 각 공간 = 한 레이아웃 + 상단(또는 사이드) 탭
- **탭 전환만.** 오라클 결과가 저널에 자동으로 꽂히거나 하는 데이터 연동은 **안 함** (나중에 필요하면 그때).

### 경로 매핑 (현재 → 통합 후)
| 현재 메뉴 | 현재 경로 | 통합 후 위치 |
|---|---|---|
| 세계관 관리 | `/world` | 🌍 세계관 · [문서] 탭 |
| 세계관 연표 | `/timeline` | 🌍 세계관 · [연표] 탭 |
| 관계도(비주얼라이저) | `/visualizer` | 🌍 세계관 · [관계도] 탭 |
| 지도(맵) | `/maps` | 🌍 세계관 · [지도] 탭 |
| 플레이 저널 | `/journal` | 🎲 플레이 · [저널] 탭 (메인) |
| 오라클 | `/oracle` | 🎲 플레이 · [오라클] 탭 |
| 세션 전투 | `/play` | 🎲 플레이 · [전투] 탭 |
| 소재 뽑기 | `/draw` | **그대로 독립** |

> 경로는 그대로 두고 **탭 UI만 얹는** 방식이라 기존 링크/북마크도 안 깨짐.

---

## 작업 순서

### 0단계: 토큰화 마무리 (통합 전에)
통합해도 이 화면들 색은 필요하니 **버리는 작업 아님.**
- [ ] `timeline` + `<Timeline>` 피처 토큰화 (라이트/다크)
- [ ] `gamebook` 에디터 토큰화 + 모바일 비율
- [ ] `gamebook/play` 토큰화 + 모바일
- [ ] (선택) `visualizer`, `maps` 토큰화 — 어차피 세계관 탭 들어갈 거라 같이

### 1단계: 세계관 공간 묶기
- 세계관은 **이미 자체 레이아웃(`world/+layout.svelte`) + 사이드바**가 있음.
- 거기에 탭(또는 사이드 항목) 추가: **문서 / 연표 / 관계도 / 지도**
- 각 탭은 기존 화면(`timeline`, `visualizer`, `maps`)을 **그 레이아웃 안에 렌더**하도록 이동/임베드.
- 가장 깔끔한 방법: `/world/timeline`, `/world/graph`, `/world/map` 같은 하위 경로로 옮기고 world 레이아웃이 감싸기.
  (또는 기존 경로 유지하고 world 사이드바에 링크만 — 더 약한 통합. 둘 중 택1)

### 2단계: 플레이 공간 묶기
- **저널을 메인**으로, 한 레이아웃에 탭: **저널 / 오라클 / 전투**
- 셋 다 이미 `currentSession` 스토어를 공유함 → 탭만 얹으면 자연스러움.
- 전투(`/play`)는 SessionFrame이 무거우니, "전투" 탭으로 들어가게만.

### 3단계: 전역 메뉴 정리
- `+layout.svelte`의 8개 `navItems` → **세계관 / 플레이 / 소재뽑기** 3개로.
- 홈 런처의 카테고리 그리드는 그대로(세계관 [문서]로 진입).

### 4단계 (나중, 별개 기능)
- [ ] **플레이 중 자료 조회 창**: 플레이 화면에서 세계관 문서를 팝업/사이드로 빠르게 검색·열람.
      (까먹은 설정 찾기용. 메뉴 통합과 무관한 작은 기능.)
- [ ] **지도 탭 본격화**: Azgaar 류 맵 제너레이터 — 통째 내장은 라이선스/덩치로 무리.
      현실적 선택지: (a) iframe 임베드, (b) 외부 링크 + 생성한 맵 이미지 불러오기.
      → 별도 논의.

---

## 별도 트랙: 의상/아이템 메이커 (★제일 나중, 여력 될 때)
> 검색 결론: **"의상만" 만드는 전문 무료 웹툴은 사실상 없음.** 옷은 항상 캐릭터 메이커에 딸려옴.
> 그래서 직접 한다면 = **페이퍼돌(레이어 겹치기) 패턴.** 단 안 무거움(에디터 아님, "겹치기 뷰어").

### 방식
- 파츠 PNG를 **z-순서로 겹쳐 합성**(몸 → 옷 → 머리 …) + **팔레트(색) 교체**로 변형.
- 우리가 만들 건 그리기 에디터가 아니라 **"파츠 골라 겹치고 색 바꾸는 뷰어"** → 가벼움.

### ⚠️ 핵심 제약
- 아무 옷이나 겹치면 틀어짐 → **같은 base(몸)에 맞춰 그려진 파츠 세트**를 써야 함.
- 그래서 "한 작가가 낸 모듈러 캐릭터+의상 세트"를 통째로 받는 게 정답.

### 에셋 = CC0 우선 (저작권 안전, 출처표기 불필요, 상업 OK)
- **Kenney (kenney.nl)** — 전부 CC0, 가입 없이 무료. 1순위.
- **OpenGameArt.org** — 양 많음. 단 에셋별 라이선스 확인(CC0만 고르기). "modular character pack" 검색.
- **itch.io** — CC0 태그 필터. 팩별 라이선스 확인.
- → PC에서 작업 직전에 **살아있는 CC0 모듈러 팩 링크**를 다시 검색해 확정(링크 바뀜).

### 우리가 만들 것
- 아이템/의상 문서는 이미 있는 `attributes` 필드로 **설정(부위·소재·등급·효과)** 정리.
- (여력 되면) 파츠 겹치기 뷰어 + 팔레트 교체로 **비주얼 조합**.
- 비주얼 소스: CC0 팩 / RPG Maker 캐릭터 제너레이터 / AI 이미지 생성 → 썸네일로 붙이기.
- 데이터/스토어 구조 변경 ❌ (오라클→저널 자동 로그 같은 연동 = 2번 b안, 지금 안 함)
- 소재뽑기 통합 ❌ (독립 유지)
- 한 방에 대격변 ❌ → 0 → 1 → 2 → 3 순서로 점진

---

## 별도 트랙: 관계도(비주얼라이저) 기능 강화
> 통합(0~3단계)이랑 **섞지 말 것.** 데이터 구조를 건드리는 일이라(2번 b안 계열),
> 관계도 탭이 통합으로 안정된 **뒤에** 단독 트랙으로 붙인다.

원하는 것 = 두 축:

### 1) 선 긋기 (관계 연결)
- 인물↔인물, 인물↔단체 등을 **선(엣지)으로 잇기** + 선에 라벨("스승", "원수", "소속").
- 비주얼라이저가 이미 노드 그래프면 → 엣지 추가/편집 기능을 얹는 형태.
- **데이터**: 관계 저장 구조 하나 필요 (`relations: { from, to, label, type }` 같은 것).
- 난이도: **중간.**

### 2) 분류 나누기 (그룹핑)
- **자동**: 카테고리(인물/단체/나라…)별 자동 색·구역, 또는 `groups`/`nations` 소속 따라 묶기.
  - 데이터 이미 있음 → 난이도 **가벼움.** (여기부터 시작 추천)
- **수동**: 빈 영역(박스)을 직접 만들고 노드를 끌어다 넣기. 자유 구획.
  - 좌표 저장 + 드래그 인터랙션 필요 → 난이도 **무거움.** (제일 나중)

### 관계도 — 대상 확장 (인물 말고도)
같은 "선 긋기" 엔진에 **대상 카테고리만 바꿔 끼우는** 방식.
- **인물망**: 인물↔인물 (스승/원수/혈연…)
- **세력망**: 단체↔단체 (동맹/적대/예속)
- **종족망**: 종족↔종족 (우호/적대/지배)
- 탭이나 토글로 "어떤 관계도를 볼지" 전환.
- 관계 데이터 구조를 카테고리 무관하게 일반화: `relations: { fromId, toId, label, type }`.

---

## 별도 트랙: 연표를 "역사책"으로 확장
> 이것도 **데이터 구조 건드림**(날짜 필드 추가) → 통합과 안 섞고 별도 트랙.

### 지금 → 목표
- 지금: `events`(사건)만 시간축에 표시.
- 목표: **인물 생몰년 / 재위기간 / 여러 사건**을 한 시간축에 얹어 "역사" 정리.

### 두 가지 서술 방식 (역사서의 두 축)
1. **편년체 (시간 흐름순)** — 연도 따라 쭉.
   - "1023 A왕 즉위 / 1025 B전쟁 / 1027 C 출생"
   - = 지금 세로 타임라인의 확장판. (사건뿐 아니라 인물 출생/사망 등도 점으로)
2. **기전체 (주체/치세 단위)** — 한 주체의 구간 안에 사건을 묶기.
   - "A왕 재위기(1023~1041) 동안 일어난 일들"
   - = **가로 띠(band)/간트 차트**: 위에 왕·인물의 재위/생몰 구간을 막대로, 그 기간에 걸친 사건을 점·선으로 얹기.

### UI 형태
- 편년체 = 세로 타임라인 (현재 거 확장)
- 기전체 = 가로 타임밴드/간트
- 연표 화면에서 **두 뷰 토글**.

### 데이터 전제 (먼저 필요)
- 사건: `date` 또는 `start~end` (걸친 기간 사건 대응)
- 인물: `생년 / 몰년`
- 왕·통치자 등: `재위 시작 / 재위 끝`
- → 인물/사건 문서에 **날짜 필드 추가**가 선행되어야 함.
- 표기 고민: 가상 세계라 **연호/책력**이 현실과 다를 수 있음 → 숫자 연도 + 선택적 연호 라벨.

### 추천 순서 (연표 트랙)
1. 사건 날짜 필드 정비 → 편년체에 사건 외(출생/사망 등)도 점으로
2. 인물 생몰년 필드 → 편년체에 인물 표시
3. 재위/기간 필드 + 기전체 띠(간트) 뷰 — 가장 무거움
4. 사설 주석 레이어 / 아이템 연결 — 아래 참고

### 연표 — 사설(주석) 레이어
- 정사(객관 사건) 흐름 옆에 **작가 해설·뒷이야기를 "사설"로** 달기. (역사책의 "사관 왈—")
- 사건에 딸린 주석 필드 or 연표 위 별도 메모 레이어.
- 정사 vs 사설을 **시각적으로 구분**(색/아이콘/들여쓰기).

### 아이템발 사건도 고려
- 사건의 주체/원인이 인물·단체만이 아니라 **아이템**일 수 있음. ("그 검 때문에 전쟁 남")
- 관계도에 **아이템 노드**, 연표 사건에 **관련 아이템 연결**.
- `mentions` 등 기존 연결로 일부 가능할지 확인 → 날짜·관계 구조 손볼 때 같이.

---

## 별도 트랙: 지도 = 다층 맵 보드 (공간축 진입)
> 세 뷰가 같은 데이터를 세 방향에서: **연표=시간축 / 관계도=관계축 / 지도=공간축.**
> 목적은 "예쁜 대륙 그림"이 아니라 **정리·설정오류 수습용 참조 이미지.**
> 핵심 요건: **단순 · 2D · 이미지 + 핀.**

### 핵심 통찰
- 세계지도 / 마을지도 / **집 평면도**는 그리는 방식은 달라도
  **"이미지 + 핀" 구조는 동일** → 기능 하나로 만들고 **이미지만 갈아끼움.**
  - 세계지도 → 핀: 나라/도시
  - 마을지도 → 핀: 건물/NPC 집
  - 집 평면도 → 핀: 방/사건 장소

### 다층(드릴다운) 구조
- 맵 1개 = `{ id, name, imagePath, pins[], parentMapId? }`
- 핀 = `{ id, x, y, label, linkDocId?, linkMapId? }`
  - `linkDocId` → 월드 문서 요약 팝업
  - `linkMapId` → **하위 맵으로 드릴다운** (세계 → 마을 → 여관 평면도)
- 구글맵 줌인 같은 느낌: 상위 맵 핀 클릭 → 하위 맵 열림. 빵부스러기(breadcrumb)로 상위 복귀.

### 맵 종류 (전부: 외부 무료 2D 툴 → 이미지 업로드 → 핀)
> **자체 에디터 안 만듦.** 있는 무료 2D 툴 가져다 쓰고, 우리는 정리·핀·계층만.
> 전부 새 탭 링크 + 이미지 업로드라 우리 부담 ≈ 0. (2D로 충분)

| 종류 | 외부 툴(가져다 쓰기) | 우리 역할 |
|---|---|---|
| 세계/지역 지도 | Azgaar Fantasy Map Generator | 바로가기 + 핀 |
| 던전/유적 미로 | Dungeon Scrawl / donjon / Watabou One Page Dungeon | 바로가기 + 핀 |
| 마을/도시 | Watabou Medieval City Generator | 바로가기 + 핀 |
| 집 평면도/가구 | 무료 브라우저 평면도 툴 | 바로가기 + 핀 |

- **링크·생존여부는 바뀔 수 있음** → PC에서 작업 전 최신 검색해서 살아있는 무료 2D 툴로 확정.
- "지도 탭"에 **외부 툴 바로가기 모음** 두고, 거기서 뽑은 이미지를 맵 보드에 올려 핀 얹기.
- (옵션·여력 되면) **던전 격자 생성기만** 자체 구현 검토 — 격자라 비교적 쉬움. 그 외 에디터는 안 만듦.

### 데이터 (가벼움)
- 맵·핀 전부 위 구조로 SQLite/localStorage에 저장. 이미지 좌표는 **상대값(0~1)** 으로 저장하면 리사이즈에 안전.

### 추천 순서 (지도 트랙)
1. 맵 이미지 업로드 + 표시 (단일 맵) + **외부 툴 바로가기 모음**(Azgaar 등)
2. 핀 찍기 + 문서 연결 + 요약 팝업
3. 다층(드릴다운) — 핀으로 하위 맵 연결 + breadcrumb
4. **던전/유적 + 마을/실내 = CC0 타일 + 격자 배치 알고리즘으로 자체 랜덤 생성** ← 가성비 최고, 자체구현 1순위
   - 타일은 CC0(Kenney/OGA 등)에서 받음 → 그림 안 그려도 됨
   - 던전=방·통로 알고리즘(BSP·드렁큰워크), 마을/실내=배치 규칙
   - 세계지도(대륙/바이옴)는 무거우니 자체생성 ❌ → Azgaar 등 외부툴
5. (선택) 핀 종류별 아이콘/색

---

## 확인 필요 (다음에)
- 비주얼라이저가 지금 **노드 그래프**인지, 어떤 라이브러리(d3? 자체?)인지 코드 확인
  → 선 긋기 얹기 난이도가 여기서 갈림
- "관계도 = `/visualizer`" / "지도 = `/maps`" 맞는지 코드로 최종 확인
- 세계관 통합을 **경로 이동**(`/world/timeline`)으로 갈지 **링크만**(약한 통합)으로 갈지 결정
- 플레이 탭을 상단 탭으로 둘지, 저널 안 토글로 둘지

---

## 별도 트랙: 시트 시스템 전체 재설계 — "블록형 시트" (★큰 거, 뼈대 변경)
> 인물만이 아니라 **전 카테고리 시트가 고정 칸** 구조라 장르·취향에 안 맞음.
> 방향 합의됨: 고정 칸 → **블록을 쌓는 자유형 + 프리셋**.

### 왜
- 장르마다 필요 정보 다름(판타지≠현대물≠TRPG). 캐릭터마다 공통+개별 특성 섞임.
- 지금: 카테고리별 전용 에디터에 칸 하드코딩(CharacterSheet=STR/HP, ItemEditor=…). 손도 못 댐.

### 개념: 블록형 시트
- 모든 문서 기본 = 빈 캔버스(공통: 제목·요약·본문·관계).
- 거기에 **블록 추가**: 스탯 / 수치(HP 등) / 자유 텍스트 / 목록 / 관계 / 이미지 …
- 블록을 **자유롭게 쌓고 순서 변경/삭제**. (마구잡이 타입에 맞음)
- 자주 쓰는 조합은 **프리셋**: "TRPG 인물"=[스탯+HP+인벤], "현대물 인물"=다른 세트.
- 지금의 전용 시트(CharacterSheet 등) → **블록 프리셋**으로 흡수.

### 이걸로 한 번에 풀리는 것
- 장르마다 다른 정보 → 블록 자유추가/프리셋
- 마구잡이 정리 → 블록 쌓기
- 관계 → 관계 블록(RelationsPanel 재활용)
- 내보내기 → 블록 순서대로 텍스트/마크다운

### 난이도 / 주의
- **GENESIS 뼈대 변경.** `attributes` 구조부터 갈아엎고 10개 에디터를 블록화.
- 기존 입력 데이터 **마이그레이션 필수**(안 깨지게).
- 단계로:
  1. 블록 데이터 구조 + 기존 attributes 마이그레이션 경로
  2. 블록 렌더러/에디터(추가·순서변경·삭제) 1개 카테고리 시범
  3. 블록 종류 차례로(텍스트→목록→스탯→수치→관계→이미지)
  4. 프리셋(장르/타입별 세트)
  5. 전 카테고리 이행 + 구 에디터 제거
  6. 내보내기(블록→마크다운)

### 다음에 먼저 정할 것
- 실제 쓰는 **블록 종류 목록**부터.
- 프리셋(장르 리스트).

---

## 별도 트랙: 내보내기 (Export)
- 문서/세계관을 **텍스트·마크다운으로 추출**(집필 메모 붙여넣기 / 공유).
- 블록형 시트 되면 자연스럽게: 블록 순서대로 마크다운 변환.
- 범위: 문서 1개 / 카테고리 / 세계관 전체.
- (기존 백업=데이터 백업용. 이건 "읽는 문서"로 빼는 것.)

---

## 시트 보강: 공통 기본 칸 + 소재뽑기 랜덤 연동
> 블록형 시트 트랙과 함께 갈 것. (칸 ↔ 소재뽑기 연결)

### 칸은 3종류로 정리
- **(가) 공통 기본 칸**: 이름·나이·성별 — 장르 무관, 모든 인물 고정 제공.
- **(나) 장르 고정 칸**: 스탯/HP 등 기존 것 — 장르별, 그대로 둠.
- **(다) 블록**: 자유 추가(블록형 시트 트랙).

### 소재뽑기 랜덤 연동 (방식 A 확정)
- 칸 옆에 🎲 → 누르면 **지정된 소재뽑기 항목에서 무작위로 뽑아 채움.**
- **방식 A**: 칸마다 "어느 소재뽑기 항목(세부분류)에서 뽑을지"를 지정.
  - 예: 성별 칸 → 소재뽑기 "성별" 항목 / 직업 칸 → "직업" 항목.
  - 정확함(성별 칸엔 성별이 뽑힘). 칸↔항목 매핑 설정이 필요.
- 작성자가 직접 적는 게 원칙, 🎲는 **막힐 때 보조**.
- 전제: 소재뽑기 구조(대분류 > 세부분류 > 태그) 그대로 활용. 칸의 매핑 = 세부분류 id 저장.

### 다음에 정할 것
- 기본 칸(이름·나이·성별) 먼저 넣을지 / 블록 트랙과 같이 갈지.
- 칸↔소재뽑기 매핑 UI(칸 설정에서 항목 고르기).

---

## 별도 트랙: 공통 기본 칸 + 소재뽑기 연동 (블록형 시트와 함께)
> 시트 재설계 때 같이 반영. 칸을 3종류로 구분.

### 시트의 칸 = 3종류
- **(가) 공통 기본 칸**: 이름·나이·성별 — 모든 인물 고정 제공. (지금은 제목=이름뿐)
- **(나) 장르 고정 칸**: 스탯/HP 등 기존 것 — 장르별, 그대로 둠.
- **(다) 블록**: 자유 추가(앞 트랙).

### 소재뽑기 연동 — 칸 옆 🎲 랜덤 채우기
- 작성자가 직접 적는 게 원칙이지만, 막힐 때 **소재뽑기 항목에서 무작위로 뽑아 채우기**.
- 칸 옆 🎲 버튼 → 지정된 소재뽑기 항목에서 랜덤 1개 → 칸에 입력.

### 연동 방식: **A 확정** (칸마다 소재뽑기 항목 지정)
- 칸별로 "어느 소재뽑기 항목에서 뽑을지" **매핑**.
  - 예: 성별 칸 → 소재뽑기 "성별" 항목 / 직업 칸 → "직업" 항목 / 성격 칸 → "성격/태도" 항목.
- 정확함(성별 칸엔 성별이 뽑힘). 대신 칸↔항목 매핑 설정이 필요.
- (참고로 안 택한 B = 대분류 통째 랜덤 = 간단하지만 부정확.)

### 전제 / 확인
- 소재뽑기 구조: 대분류 > 항목 > 태그(값). 시트 칸은 **항목 단위**에 연결.
- 블록형 시트의 블록(특히 텍스트/선택 블록)에도 같은 🎲 매핑 적용 가능하게 설계.
